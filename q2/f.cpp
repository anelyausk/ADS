//first sort arr
//pick mid element - it would be root of tree
//on left and right side - eq elements

/*build (l, r)
mid = (l+2)/2
build(l, mid-1)
build(mid+1, r)
*/

/*
merge sort
read everything
implement recursive func
out - prints vertices in correct order

*/